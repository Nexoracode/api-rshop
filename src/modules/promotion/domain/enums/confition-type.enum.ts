export enum ConditionType {
    USER = 'user',
    PRODUCT = 'product',
    CATEGORY = 'category',
    VARIANT = 'variant',
    MIN_ORDER_AMOUNT = 'min_order_amount',
    FIRST_ORDER = 'first_order',
}
